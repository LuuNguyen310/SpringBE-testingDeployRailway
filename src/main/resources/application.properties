spring.application.name=KItchen-Control-BE

# Server Port
server.port=8080

# C?U HÌNH POSTGRESQL (Dùng bi?n môi tr??ng là t?t nh?t)
# L?u ý: Mình ?ã xóa ?o?n encrypt=true (c?a SQL Server) ?i vì Postgres không c?n
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://springdb.railway.internal:5432/railway}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:MatKhauCucManh123}
spring.datasource.driver-class-name=org.postgresql.Driver

# C?U HÌNH JPA/HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Thêm dòng này ?? Hibernate hi?u rõ ti?ng nói c?a Postgres
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
